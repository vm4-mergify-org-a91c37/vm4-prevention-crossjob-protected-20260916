merge_queue:
  mode: serial
  max_parallel_checks: 1

queue_rules:
  - name: default
    queue_conditions:
      - base = vm4-mq-shared-base-a-20260916
    merge_conditions:
      - check-success = VM4_MQ_SHARED_HOLD_NEVER
    merge_method: merge
